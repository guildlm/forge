"""Domain plugins for Forge.

Plugins carry *domain conventions* (e.g. which file types a guild cares about,
domain-specific extraction or filtering helpers). The core engine never imports
domain knowledge directly; it is supplied here and wired in via configuration.
"""
